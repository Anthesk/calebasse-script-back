services:
  web:
    build: .
    volumes:
      - .:/usr/src/app
    ports:
      - "5000:3000"
  mongodb:
    image: mongo:5.0
    container_name: mongo
    restart: unless-stopped
    ports:
        - 27017:27017
    volumes:
        - /data/db

